{
  "buildCommand": "cd apps/crm-frontend && npm install --legacy-peer-deps && npm run build",
  "outputDirectory": "apps/crm-frontend/dist",
  "installCommand": "npm install --legacy-peer-deps",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}