{
  "name": "penny-debt-monorepo",
  "version": "2.0.0",
  "private": true,
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "install-all": "npm install",
    "dev:crm": "npm --prefix apps/crm run dev",
    "dev:website": "npm --prefix apps/website run dev",
    "dev:backend": "npm --prefix apps/backend run dev",
    "build": "npm run build:website && npm run build:crm && npm run build:backend",
    "build:crm": "npm --prefix apps/crm run build",
    "build:website": "npm --prefix apps/website run build",
    "build:backend": "npm --prefix apps/backend run build || echo 'backend has no build step'",
    "build:mobile": "echo 'build mobile in local environment via expo or EAS'"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "vite": "^5.1.2"
  }
}
