{
  "buildCommand": "npm install --legacy-peer-deps && npm run build:website",
  "outputDirectory": "apps/website/dist",
  "installCommand": "npm install --legacy-peer-deps",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
